<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="listener"
            type="id.ac.ui.cs.mobileprogramming.mrizkyanditama.sportrack.ui.adapter.NewsAdapter.NewsListener" />

        <variable
            name="news"
            type="id.ac.ui.cs.mobileprogramming.mrizkyanditama.sportrack.model.NewsArticle" />

    </data>

    <androidx.cardview.widget.CardView
        style="@style/NewsArticle.View"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardElevation="@dimen/card_elevation"
        android:onClick="@{()->listener.onClick(news)}"
        >

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/drawable_list_item">

            <!-- News block -->
            <RelativeLayout
                android:id="@+id/newsImageView"
                android:layout_width="match_parent"
                android:layout_height="180sp">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/newsImage"
                    style="@style/NewsArticle.Image"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/image_size"
                    android:contentDescription="@string/news_image"
                    tools:src="@drawable/ic_pic"
                    app:sportTypeImage="@{news.urlToImage}"
                    />

                <TextView
                    android:id="@+id/newsAuthor"
                    style="@style/NewsArticle.Author"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_alignParentBottom="true"
                    tools:text="ABC News"
                    android:text="@{news.source.name}"
                    />

            </RelativeLayout>

            <!-- News Title -->
            <TextView
                android:id="@+id/newsTitle"
                style="@style/NewsArticle.Title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@+id/newsImageView"
                tools:text="Elon Musk says Tesla can't yet launch in India"
                android:text="@{news.title}"
                />

            <!-- Publish at -->
            <TextView
                android:id="@+id/newsPublishedAt"
                style="@style/NewsArticle.PublishedAt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@+id/newsTitle"
                android:layout_marginStart="@dimen/normal_padding"
                android:layout_marginLeft="@dimen/normal_padding"
                tools:text="@string/str_test_date"
                android:text="@{news.publishedAt}"
                />

        </RelativeLayout>
    </androidx.cardview.widget.CardView>

</layout>